<Window x:Class="Checkout.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Checkout"
        mc:Ignorable="d"
        Title="CheckOut" Height="220" Width="260">
    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label>Produkter</Label>

        <ListBox Grid.Row="1" x:Name="lstProducts"
         VirtualizingStackPanel.IsVirtualizing="False"
         AutomationProperties.AutomationId="lstProducts"/>

        <StackPanel Grid.Row="1" Grid.Column="1" Margin="5,0,0,0">
            <Button x:Name="btnAddCoffee" 
        AutomationProperties.AutomationId="btnAddCoffee"
        Margin="0,0,0,5" Click="ButtonAddCoffee">Kaffe (49 kr)</Button>
            <Button x:Name="btnClear" 
        AutomationProperties.AutomationId="btnClear"
        Click="ButtonClearCart">Rensa kundvagn</Button>
        </StackPanel>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,0">
            <TextBlock Text="Total Pris:" VerticalAlignment="Center" FontWeight="Bold"/>
            <TextBlock x:Name="txtTotal" Text="0 kr" FontWeight="Bold" Margin="5,0,0,0"/>
        </StackPanel>


    </Grid>
</Window>
